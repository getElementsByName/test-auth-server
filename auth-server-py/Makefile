PORT=8080
JWT_HMAC_SECRET=test
ENVOY_RATELIMIT_HTTP_URL=http://localhost:8080/json

start:
	JWT_HMAC_SECRET=$(JWT_HMAC_SECRET) ENVOY_RATELIMIT_HTTP_URL=$(ENVOY_RATELIMIT_HTTP_URL) uvicorn main:app --reload --port=$(PORT)